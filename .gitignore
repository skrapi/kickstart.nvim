tags
test.sh
.luarc.json
nvim

spell/

# This is a personal fork, so the `vim.pack` lockfile is tracked in version
# control (see `:help vim.pack-lockfile`). The official `nvim-lua/kickstart.nvim`
# repository leaves it ignored to avoid unneeded merge conflicts.
# nvim-pack-lock.json

.DS_Store
